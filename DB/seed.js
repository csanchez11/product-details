// const Products = require('./models.js');  for seeding data function at bottom
// const db = require('./index.js');



const features = [Buttery-soft,];
const materials = [];
const designedFor = [];
const whyMade = [];


const createProduct = () => {
  //create individual prod
};

const createProducts = () => {
  //create Array of prod

const seedData = function() {
  // Complete me please
};

// NOTE: DO NOT invoke this function as part of your
// server code - it is meant to only be run once so that
// you have access to data to work with
console.log("Test Passed to see if seeding is capable: next invoke seedData");